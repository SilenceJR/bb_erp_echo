# 项目代理协作规则

## 子代理与模型路由

主代理负责需求拆解、API 契约、任务协调和最终验收。

根据任务类型选择代理：

- 常规 Go 后端实现：`go_backend`
- Go 架构、并发、事务、安全或疑难性能问题：`go_architect`
- UI/UX、信息架构、设计系统和交互方案：`ui_designer`（只读设计）
- 新页面、复杂交互和响应式实现：`web_frontend`
- UI 一致性、业务体验、响应式和可访问性验收：`ui_reviewer`（只读审查）
- 小型前端修改、接口接入或明确 Bug：`web_maintainer`
- 大范围代码搜索和调用链定位：`code_explorer`
- 跨模块业务目标、用户流程、权限范围、验收标准和发布价值的最终产品审查：`product_manager`（只读审查）

不要为简单搜索或机械修改调用高成本代理。

跨前后端任务：

1. 主代理先确定 API 契约。
2. `code_explorer` 可以先定位相关代码。
3. 后端由 `go_backend` 实现。
4. 新页面由 `web_frontend` 实现；普通接入由 `web_maintainer` 实现。
5. 架构风险交给 `go_architect` 只读审查。
6. 写代理不得同时修改相同文件。
7. 主代理等待所有结果并完成最终验证。

UI 任务固定流程：

1. `ui_designer` 阅读业务、权限和现有设计系统，输出按优先级排序的设计说明与验收标准，不修改代码。
2. 主代理确认设计范围和 API 契约，避免设计与业务能力脱节。
3. `web_frontend` 根据设计说明实现 Vue、Element Plus、设计令牌和响应式交互。
4. 主代理完成类型检查、生产构建和必要的浏览器运行态验证。
5. `ui_reviewer` 独立验收；存在 P0/P1 时退回 `web_frontend` 修正，复验通过后才能合并主分支。
6. `product_manager` 在技术/UI/专项审查完成后进行最终产品审查；存在 P0/P1 或未满足发布前置条件时不得交付，主代理负责汇总证据并完成最终验收。

设计、实现、审查代理不得同时修改同一文件；`ui_designer` 和 `ui_reviewer` 始终只读。
`product_manager` 始终只读，重点判断业务闭环与发布价值，不替代技术、UI 或运行态验收。

## 分支开发约定

- 功能、修复和发布工程变更统一在 `codex/<主题>` 分支开发，不直接修改 `main`/`master`。
- 分支完成 Go、Web、Tauri 及相关 CI 配置验证后，再合并到主分支。
- Gitee 为主代码仓库时，从 Gitee 创建并推送功能分支与版本标签；GitHub 只接收镜像并执行验证、构建和发布任务。

## 代码与文档同步提交

- 每次修改 Go 代码，必须在同一个工作项和同一个提交中同步更新受影响的状态、API、配置、部署或发布文档。
- API、权限、请求/响应字段或错误行为变化时，必须同步 Handler Swagger 注释、`docs/API.md`、OpenAPI 产物，必要时更新 `test.http`。
- Web/Tauri 共用 API 或运行约束变化时，同时检查 `docs/WEB_STATUS.md`、`client/API_SYNC.md` 和客户端构建规则。
- 每次完成代码后更新 `docs/BACKEND_STATUS.md` 的已完成、待完成、待修改、待修复、已发现问题和验证记录；不把未验证内容写成已完成。
- 提交前按变更范围执行测试，并在交接信息中分别报告代码变更、文档同步、验证结果和提交结果；除非用户明确要求，否则不得把代码和对应文档无理由拆成不同提交。
