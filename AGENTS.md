# 项目代理协作规则

## 子代理与模型路由

主代理负责需求拆解、API 契约、任务协调和最终验收。

根据任务类型选择代理：

- 常规 Go 后端实现：`go_backend`
- Go 架构、并发、事务、安全或疑难性能问题：`go_architect`
- UI/UX、信息架构、设计系统和交互方案：`ui_designer`（只读设计）
- 新页面、复杂交互和响应式实现：`web_frontend`
- UI 一致性、业务体验、响应式和可访问性验收：`ui_reviewer`（只读审查）
- 小型前端修改、接口接入或明确 Bug：`web_maintainer`
- 大范围代码搜索和调用链定位：`code_explorer`
- 跨模块业务目标、用户流程、权限范围、验收标准和发布价值的最终产品审查：`product_manager`（只读审查）

不要为简单搜索或机械修改调用高成本代理。

跨前后端任务：

1. 主代理先确定 API 契约。
2. `code_explorer` 可以先定位相关代码。
3. 后端由 `go_backend` 实现。
4. 新页面由 `web_frontend` 实现；普通接入由 `web_maintainer` 实现。
5. 架构风险交给 `go_architect` 只读审查。
6. 写代理不得同时修改相同文件。
7. 主代理等待所有结果并完成最终验证。

UI 任务固定流程：

1. `ui_designer` 阅读业务、权限和现有设计系统，输出按优先级排序的设计说明与验收标准，不修改代码。
2. 主代理确认设计范围和 API 契约，避免设计与业务能力脱节。
3. `web_frontend` 根据设计说明实现 Vue、Element Plus、设计令牌和响应式交互。
4. 主代理完成类型检查、生产构建和必要的浏览器运行态验证。
5. `ui_reviewer` 独立验收；存在 P0/P1 时退回 `web_frontend` 修正，复验通过后才能合并主分支。
6. `product_manager` 在技术/UI/专项审查完成后进行最终产品审查；存在 P0/P1 或未满足发布前置条件时不得交付，主代理负责汇总证据并完成最终验收。

设计、实现、审查代理不得同时修改同一文件；`ui_designer` 和 `ui_reviewer` 始终只读。
`product_manager` 始终只读，重点判断业务闭环与发布价值，不替代技术、UI 或运行态验收。

## 分支开发约定

- 功能、修复和发布工程变更统一在 `codex/<主题>` 分支开发，不直接修改 `main`/`master`。
- 分支完成 Go、Web、Tauri 及相关 CI 配置验证后，再合并到主分支。
- Gitee 为主代码仓库时，从 Gitee 创建并推送功能分支与版本标签；GitHub 只接收镜像并执行验证、构建和发布任务。

## 代码与文档同步提交

- 每次修改 Go 代码，必须在同一个工作项和同一个提交中同步更新受影响的状态、API、配置、部署或发布文档。
- API、权限、请求/响应字段或错误行为变化时，必须同步 Handler Swagger 注释、`docs/API.md`、OpenAPI 产物，必要时更新 `test.http`。
- Web/Tauri 共用 API 或运行约束变化时，同时检查 `docs/WEB_STATUS.md`、`client/API_SYNC.md` 和客户端构建规则。
- 每次完成代码后更新 `docs/BACKEND_STATUS.md` 的已完成、待完成、待修改、待修复、已发现问题和验证记录；不把未验证内容写成已完成。
- 提交前按变更范围执行测试，并在交接信息中分别报告代码变更、文档同步、验证结果和提交结果；除非用户明确要求，否则不得把代码和对应文档无理由拆成不同提交。

## 长期默认执行纪律

本节是所有代理的长期默认约束。用户当次明确要求优先；没有明确要求时，不得依赖用户重复提醒。

### 共同交付目标

- 面向真实工厂用户交付可长期使用的产品，不以“能运行”“有页面”或“测试通过”代替业务可用性。
- Web 与 Tauri 共用 `web/src` 的业务页面、组件、设计令牌和 API 语义；平台差异集中在 Tauri/Rust、系统能力和安全边界，禁止复制业务页面形成双实现。
- 已确认不再使用的旧前端页面、旧组件、旧 CSS 类、兼容令牌和展示双轨不保留；设计升级应收敛到共享组件与令牌，删除无调用遗留。此规则不授权删除旧数据库数据、破坏 API/升级兼容或取消已发布契约。
- 桌面客户端优先，主验收覆盖 Windows/Tauri 与桌面 Web；平板和手机只做低频访问适配，不能反向破坏桌面信息密度和操作效率。
- 静态代码检查、单元测试、类型检查、生产构建、浏览器运行、Tauri 运行、Windows/WebView2 和生产验收是不同证据层级，未执行的层级不得写成已通过。
- 设计、实现和审查不能由同一代理自证通过。子代理“完成”不等于产品完成，主代理必须复核差异和证据。

### 主代理

1. 开始前确认当前目录、分支、工作树、远端、用户未提交内容及适用说明。
2. 将需求明确为业务目标、角色、入口、允许/禁止变更、API/权限契约、验收标准和验证层级。
3. 跨前后端任务先锁定请求、响应、错误码、权限、状态和兼容边界。
4. 为写代理分配互斥的文件或模块所有权，并告知其不是独自在代码库工作，不得回退其他修改。
5. 汇总结果后亲自完成差异检查、构建、测试、必要的真实运行态验证和文档一致性检查。
6. 任一技术、UI 或产品审查出现 P0/P1，必须退回修正并复验后再交付。
7. 交付时分别报告代码变化、文档同步、已验证、仅静态验证、未验证、分支/提交/推送状态和剩余风险。

### 文件所有权和协作安全

- 同一时间不得让两个写代理修改同一文件或紧耦合模块；每个写代理只改明确授权范围。
- 发现必须跨界时先报告，不得顺手扩大改动。`web_maintainer` 遇到跨页面设计、整体布局或复杂响应式时应交回 `web_frontend`。
- 保留用户与其他代理已有修改，禁止用 checkout、reset、整文件覆盖或无关目录格式化清除差异。
- 脏工作树先识别归属；能绕开则继续，存在实质冲突才请求用户决定。
- 子代理回报必须包含具体文件/符号、完成内容、验证命令和结果、未验证项与剩余风险。

### UI/UX 产品质量基线

- 目标是现代、克制、耐用的桌面 App，不套用老式管理后台，不依赖大圆角、重阴影、高饱和色块和过多卡片制造“塑料感”。
- 视觉层级主要依靠排版、间距、表面层级、细边框和有限品牌色；不得为“高级感”牺牲 ERP 扫描效率、表格容量、对齐和明确性。
- 每页检查窗口、导航、中央区、详情、标题、工具栏、表格和分页比例；同一容器标题、正文、分隔线、表单和底栏必须共享可解释的左右基线。
- 详情标签列固定且克制，内容列获得主要宽度；长中文、地址、编号和空值必须可读。窄侧栏表单优先单列，只有字段宽度足够时才双列。
- 固定底栏不得遮挡最后字段；滚动内容需留安全空间。必测 `1024×680`、1280、1440、1920 及断点两侧。
- 左栏支持完整/图标/隐藏，状态本机持久化；隐藏后保留恢复入口，图标态有 Tooltip 和无障碍名称。
- 标准停靠详情约 420px，图片密集型可 520px；中央内容仍能保持约 720px 才停靠，否则用覆盖 Drawer。同一详情不得出现两个载体。
- 停靠详情使用克制的空间过渡，中央区域点击不关闭，避免选择/复制误关；覆盖 Drawer 可遮罩和 Escape 关闭。创建/编辑聚焦首个可编辑字段，关闭恢复触发点。
- 动效用于解释空间和状态：常规约 120–180ms，布局/面板约 160–220ms，支持 `prefers-reduced-motion`，禁止持续漂浮、弹跳、过度缩放和多层动画。
- Element Plus Dialog/Drawer/Popover 等 Teleport 浮层由组件库管理生命周期、焦点和滚动；正文首帧必须可读，禁止再包初始 `opacity: 0` 的 Motion/AnimatePresence。
- 亮色、暗色和全部品牌色均检查文本、边框、焦点、primary/plain、Tooltip、消息和浮层；暗色不能出现突兀白块。
- 键盘路径覆盖 Tab/Shift+Tab、Escape、焦点环、焦点循环和关闭后的焦点恢复；主要点击目标约 40px，图标按钮有可访问名称。
- 加载、空、错误、无权限、禁用、只读、成功、危险、模块未初始化必须分开表达；不可用数据不得显示为真实零。
- 正常服务器连接属于低频信息，放在设置；只有检测中、失败、身份失效或服务不可用时提升为全局提示。
- Figma 只允许写入可确认的免费个人 Draft；创建、回读、二次修改、截图和可查看链接不能完整通过时立即停止，不写团队文件，改用书面设计规范。
- Eagle 仅只读筛选字体、图标和设计参考；本工厂内部使用场景不执行商业授权检查或阻断，只评估视觉一致性、格式质量、中文/数字可读性和 WebView/Tauri 稳定性，不合适的素材不强制采用。

### 权限、初始化和暂缓模块底线

- 后端鉴权是最终边界，前端显隐仅改善体验。管理写权限可读取完成管理流程所需的数据，但不能因此扩大写权限或组织范围。
- 权限写入防止自我提权、跨组织管理、授予操作者不拥有的权限、无效 ID 部分写入和最后一个有效超级管理员失效；高风险检查在事务中基于当前状态重验并考虑并发。
- 系统托管初始化账号（如 `Silence`）不得出现在普通账号管理列表，也不能通过管理接口修改；密码只保存安全哈希，不得出现在日志、前端、API/状态文档或测试快照。
- 暂缓自动建表不等于删除旧表/数据。全新数据库缺少任务单、仓库、供应商等结构时返回明确模块状态并禁用写操作；已有数据库可工作的路径保持。
- 统计来源缺失必须标识不可用来源，占位零值不能呈现为真实经营指标；跨模块删除保护、附件、首页统计、库存和种子数据对缺表默认失败关闭。

### 审查等级和证据

- P0：数据破坏、越权、安全漏洞、无法启动或核心流程不可用，阻断交付。
- P1：主要流程中断、真实窗口布局不可用、关键状态误导、严重可访问性或兼容问题，必须修复并复验。
- P2：不阻断核心流程的优化或目标环境补验，记录边界，不伪装成完成。
- UI 截图须覆盖代表性完整窗口和真实状态，不能用孤立组件截图替代整体比例审查。
- 最终审查结论为“通过”“有条件通过”或“不通过”，并列明实际证据、人工验收和发布前置条件。
