# Go 后端状态、进度与维护台账

> 基准日期：2026-08-27
> 适用范围：cmd/、internal/、docs/API.md、Go 相关 CI 与发布脚本
> 状态原则：已完成表示代码已实现并有对应测试或构建证据；待完成表示已列入当前交付范围但尚未闭环；未完成表示尚未实现或明确不属于首版；待修改表示已有实现但维护性、契约或部署前置条件仍需调整；待修复表示已确认的缺陷。

## 1. 当前结论

Go 后端核心业务已经形成可运行的模块化单体：Echo HTTP 服务、SQLite WAL、JWT/Casbin 认证授权、审计、基础资料、库存、模具、任务单、统计、受保护图片和客户端更新服务均已接入应用装配与 API 文档。

当前不能称为“生产验收完成”。本机 go vet ./... 在隔离缓存目录下通过；除 internal/update 外的 Go 测试通过；更新模块包含 httptest.NewServer 的测试受当前沙箱禁止 IPv6 本地监听影响，导致本机 go test ./... 未能全量通过。GitHub Actions 的 Go job 仍以 go mod tidy、go vet ./...、go test ./... 作为正式验证入口。

本次发布以当前 `main` 代码建立正式 `0.0.2` 基线，随后以发布验收文档增量建立 `0.0.3`，不新增后端业务 API。正式发布、公开清单、Windows 构建和真实升级尚未验收完成；在 CI、匿名下载校验和 Windows 10/11 用户验收完成前，仍保持“发布环境闭环：待完成”。

首次 `v0.0.2` Windows CI 暴露 Tauri 更新命令在 Windows 专属编译路径中的 Rust `Result` 错误类型推断问题，已补充显式类型，待重新推送验证；该问题不改变 API 或更新流程设计。

## 2. 进度总表

| 领域 | 状态 | 当前说明 | 依据 |
| --- | --- | --- | --- |
| 服务启动与配置 | 已完成 | 配置默认值、BB_ERP_ 环境变量覆盖、健康检查、就绪检查和优雅关闭已接入。 | internal/app、internal/config |
| SQLite 数据层 | 已完成 | 自动建表/迁移、WAL、外键、忙等待和持久化 refresh 会话已接入；当前按单机内网场景设计。 | internal/database、internal/model |
| 登录认证 | 已完成 | JWT 登录、2 小时 access token、活跃 30 天 refresh token 轮换、退出撤销、账号状态检查、当前账号修改密码和密码版本失效校验已实现。 | internal/auth、internal/middleware/auth.go |
| 角色与权限 | 已完成 | Casbin RBAC、权限初始化、角色/权限配置和组织/部门数据范围校验已实现。 | internal/role、internal/user |
| 审计与请求日志 | 已完成 | 写操作审计、个人账号/部门终端账号身份记录、结构化日志和请求 ID 已接入。 | internal/audit、internal/middleware、internal/logger |
| 客户、联系人、供应商 | 已完成 | 列表、分页、搜索、创建、更新/删除及联系人电话明细已实现。 | internal/customer、internal/contact、internal/supplier |
| 仓库与资料 | 已完成 | 单仓库目录、库位、物料和产品资料接口已实现，并保留兼容旧路径。 | internal/warehouse、internal/material、internal/product |
| 库存业务 | 已完成 | 入库、出库、调拨、立即/单据过账、冲销、幂等、库存不足校验、余额、流水和加权平均成本已实现。 | internal/inventory、internal/warehouse |
| 模具生命周期 | 已完成 | 台账、借出、归还、维修、保养、状态转换和履历已实现。 | internal/mold、internal/model/mold.go |
| 任务单流转 | 已完成 | 生产/通用任务、派发、暂停、恢复、加急、正常/强制完成、部门子任务和流转日志已实现。 | internal/workorder |
| 统计报表 | 已完成 | 首页汇总、库存、任务、模具、业务资料、审计和低库存/需保养数据聚合已实现。 | internal/statistics |
| 图片文件 | 已完成 | 受保护单图/多图上传、格式/大小校验、批量全有或全无入库、预览、替换、删除和业务权限继承已实现。 | internal/file |
| 服务端更新检查 | 已完成 | 清单拉取、版本比较、ZIP 校验、缓存、并发合并、状态保留和管理员检查接口已实现。 | internal/update/manifest.go |
| Tauri v2 客户端更新支持 | 已完成 | 签名 payload、NSIS/Portable/差分资源、SHA-256、Range、内容寻址缓存和完整包兜底已实现。 | internal/update/client_v2.go、internal/update/handler.go |
| API 文档 | 已完成 | Handler Swagger 注释、docs/API.md 和 OpenAPI 产物已维护；接口变化仍需同步更新。 | docs/API.md、docs/docs.go |
| Go 单元/集成测试 | 已完成 | 全量 `go test -count=1 ./...` 已在正常本地测试环境通过，包含更新服务网络测试。 | internal/**/*_test.go |
| 生产运行态验收 | 待完成 | 尚未完成真实配置、真实账号、内网访问、权限边界和异常场景的完整验收。 | 见第 4 节 |
| 备份与恢复 | 部分完成 | 已在本地隔离临时目录完成 SQLite 完整性备份/恢复和 1 个上传文件的 SHA-256 恢复比对；生产配置与更新缓存尚未有可演练的实际来源。 | 见第 4 节与验证记录 |
| 发布环境闭环 | 进行中 | 按 `0.0.2 → 0.0.3` 顺序发布；Windows Rust 编译问题已修复并通过 CI，随后发现发布环境中的 Tauri 公钥缺少 Minisign 注释行，CI 已增加严格校验和兼容补齐逻辑，待重新完成 Windows 正式打包；正式版与 RC/手动便携客户端 Artifact 的 Windows 10/11 安装、启动、升级、回滚和断网验收仍需完成。 | docs/GITEE_RELEASE.md |

## 3. 已完成内容

### 3.1 服务基础设施

- cmd/server 已负责装配配置、数据库、权限服务、业务 Handler、更新服务、Swagger 和 Web 静态文件。
- /health 与 /ready 已提供；服务启动时执行数据库连接、自动迁移、默认组织/角色/权限/管理员初始化。
- SQLite 使用 WAL、外键和 busy_timeout；连接池限制为单写连接，符合当前单机内网定位。
- 配置支持默认值和 BB_ERP_ 环境变量覆盖；JWT 使用系统内部密钥，管理员先使用默认密码登录并在系统内修改；更新签名公钥仍由发布配置提供。

### 3.2 认证、权限与审计

- 登录返回 JWT、refresh token 和当前账号信息；受保护 API 通过 Bearer Token 校验账号状态，refresh token 只保存 SHA-256 摘要并在成功续期后轮换。
- `/api/v1/auth/refresh` 支持活跃会话自动续期，`/api/v1/auth/logout` 撤销 refresh token；修改密码会在同一事务中撤销该账号全部 refresh 会话。
- Casbin 权限以 API object/action 表达，当前 action 为 read/write；中间件同时校验组织和部门数据范围。
- cost:view 独立控制采购单价、平均成本、库存金额和统计金额，后端响应会主动裁剪无权限字段。
- 写操作审计记录账号类型、组织、部门、终端、请求 ID、路径、状态和结果；部门终端账号的具体人员显示为“未知”。

### 3.3 业务闭环

- 基础资料已覆盖客户、联系人、供应商、仓库、库位、物料和产品。
- 库存数量使用四位定点整数，金额和单价使用分；转换和加权平均成本计算在后端完成，避免浮点误差。
- 库存单据支持草稿、过账和冲销；Idempotency-Key 用于避免重复创建同一业务请求。
- 模具操作必须经过合法状态转换并记录履历；任务单主状态由办公室流程控制，部门只操作自己的子任务状态。
- 图片文件按业务 owner 继承权限，路径经过安全校验，上传文件名不直接作为存储文件名；创建接口以重复 `file` 字段接收单图/多图，批量保存失败会清理本批次文件并回滚记录。

### 3.4 更新与发布支撑

- 服务端保留旧版客户端 ZIP 接口，兼容已有客户端。
- v2 更新清单在服务端验签后才进入内容寻址缓存；差分资源失败时降级使用完整包，不覆盖上一份可用状态。
- Windows 发布由 GitHub Actions 构建，正式标签使用 GitHub Secrets 中的 Tauri 私钥和私钥密码；后端只保存并使用公钥验签。预发布标签和手动构建仅生成独立便携客户端 Artifact，不启用正式版更新签名产物。
- Gitee 发布脚本只接受不带预发布标识的正式标签，并在更新正式版 manifest 前复验附件下载、大小、SHA-256、签名、版本递增和增量基线。

## 4. 当前待完成

按优先级排列：

1. **P0：完成生产配置检查。** 首次启动后必须修改默认管理员密码；同时明确 BB_ERP_UPDATE_*、上传目录、日志目录、CORS 和数据库路径，并确认文件目录权限。
3. **P1：完成后端真实运行态验收。** 覆盖登录、账号停用、角色权限、组织/部门边界、客户资料、库存过账/冲销、模具状态、任务流转、图片权限和更新状态接口。
4. **P1：建立并演练备份恢复。** 至少覆盖 SQLite 数据库、图片上传目录、生产配置和更新缓存；验证恢复后的权限、图片关联、库存余额和更新状态。
5. **P1：完成发布闭环验收。** 在 Windows 10/11 验证正式版 NSIS/便携版安装更新，以及 RC/手动便携 EXE 独立启动、断网、损坏资源、不可写目录和回滚；确认旧客户端仍可使用兼容 ZIP。
6. **P2：同步 API 变更链路。** 任何 Handler、请求/响应字段、权限或路径变化都必须同步 Swagger 注释、docs/API.md、OpenAPI 产物和必要的 test.http 场景。

## 5. 计划添加功能

以下属于规划项，不能在当前版本说明为已实现：

- 统计报表导出，沿用现有统计权限并确保成本字段继续受 cost:view 控制。
- 任务实时提醒或通知，先明确通知渠道、去重、失败重试和账号/部门范围，再设计接口。
- 办公室与部门权限进一步细化，补充更精确的任务、库存和资料数据范围规则。
- 可验证的自动备份、恢复检查和备份保留策略，避免只提供文件复制命令而没有恢复演练。
- 多组织、多仓库和组织管理界面属于后续架构扩展；首版仍保持单组织、单仓库边界，不应提前伪装成已支持。

## 6. 未完成与边界

- 未完成多组织切换、组织管理菜单和多仓库业务；当前模型保留组织字段，但首版业务按默认组织和单仓库运行。
- 未完成统计导出、任务实时提醒和自动备份恢复产品功能。
- 未完成真实生产网络下的压测、长期运行、数据库备份恢复和跨平台异常演练。
- 未完成 Windows 10/11 与 macOS Tauri 运行态验证；这些属于 Client/发布联调，但会直接影响后端更新接口的最终验收。

## 7. 待修改

- 清理 internal/customer/routes.go 中历史“骨架路由”命名和注释。当前应用实际注册的是 customer.NewHandler，该文件容易让维护者误以为客户接口仍未实现。
- 同步清理模型中 Customer、Contact 等“骨架模型/后续承载”旧注释，改为当前实际能力和边界说明。
- 评估图片替换/删除流程中“物理文件删除发生在数据库事务提交前”的一致性窗口；当前代码已在失败路径回滚数据库记录，但数据库提交失败时仍存在极小的物理文件与记录不一致风险。
- 为高频库存余额、库存流水和单据列表评估统一分页与查询过滤策略，避免数据量增长后接口固定全量或固定 500 条返回。

## 8. 待修复

当前没有从已执行的业务测试中确认的后端逻辑 P0/P1 缺陷。

需要先在非沙箱环境复验的测试阻断不归类为产品缺陷：本机全量测试中的 httptest.NewServer 无法绑定 [::1]:0，是当前执行环境禁止本地 IPv6 监听导致的验证阻断。

## 9. 已发现问题与风险

| 问题 | 影响 | 当前处理 |
| --- | --- | --- |
| 受限沙箱无法完成本地测试监听 | 默认执行环境不能代表更新服务网络测试结果 | 已在正常本地测试环境使用隔离缓存完成全量 Go 测试 |
| 默认 JWT 密钥和管理员密码仍可用于开发启动 | 首次登录后若不修改管理员密码会造成安全风险 | 登录后通过系统用户管理修改默认密码；JWT 按当前内部密钥策略运行 |
| SQLite 适合单机内网，不适合直接扩展为高并发多实例 | 多实例写入、共享文件系统和故障切换能力不足 | 保持首版边界；后续如扩展需重新评估数据库方案 |
| 更新签名私钥/密码依赖发布环境 Secrets | Secrets 缺失会导致正式 Windows 签名构建失败 | CI 已在正式标签构建前校验；私钥应保留加密离线备份 |
| 图片物理文件和数据库事务不是同一原子资源 | 极小概率下可能出现记录与文件不一致 | 暂列待修改，后续设计补偿清理或提交后删除 |
| 历史兼容路径与新路径并存 | 维护接口时可能只改一条路径或只更新一份文档 | API 变更必须同时检查新旧路径和 OpenAPI 文档 |

## 10. 验证记录

### 已通过

    go vet ./...                         通过（使用临时 GOCACHE/GOTMPDIR）
    Go 测试（排除 internal/update）       通过
    图片单图/多图批量接收与原子回滚测试      通过（internal/file，含 race）
    bash scripts/test-release-semver.sh  通过
    bash scripts/test-release-delta-base.sh 通过

### 当前阻断或尚未执行

    go test -count=1 ./...                 通过（正常本地测试环境；含 internal/update 网络测试）
    go mod tidy + git diff 检查            本次仅做状态核对，未修改 Go 依赖
    真实 HTTP 服务验收                    尚未执行
    SQLite/上传文件隔离备份恢复演练        通过（本地临时目录；SQLite integrity_check、上传文件 SHA-256）
    生产配置备份恢复演练                  尚未执行：JWT 为内部配置，管理员密码在系统内修改，未发现生产配置文件
    更新缓存备份恢复演练                  尚未执行：当前工作区不存在 updates/ 缓存目录
    生产配置与目录权限检查                未完成：更新变量未设置；目录 755、文件 644，需按服务账号收紧
    Windows 10/11 正式版与 RC 便携包验收   尚未执行

GitHub Actions 的 Go 验证定义在 .github/workflows/ci.yml，正式发布顺序和 Secrets 约束见 docs/GITEE_RELEASE.md。

## 11. 后续代码变更同步规则

以后每次修改 Go 代码，都必须在同一个工作项和同一个提交中同步更新受影响文档：

1. API、权限、请求/响应字段或错误行为变化：同步 Handler Swagger 注释、docs/API.md、docs/docs.go、docs/swagger.json、docs/swagger.yaml，必要时更新 test.http。
2. 配置、部署、数据库、更新或发布行为变化：同步 README、本文档和 docs/GITEE_RELEASE.md 等相关文档。
3. 业务实现完成、验证阻断、已发现问题或待办状态变化：更新本文档的状态表、验证记录和后续顺序。
4. Web/Tauri 共用 API 变化：同时检查 docs/WEB_STATUS.md、client/API_SYNC.md 和客户端构建约束。
5. 提交前执行与变更范围匹配的测试，并在提交说明或交接记录中分别写明代码、文档、验证和提交状态；代码与对应文档不得无理由拆成不同提交。

本规则只要求同步项目范围内的相关文档，不把 .codex、个人临时文件或无关输出纳入提交。

## 12. 架构与时序图

- [Go 后端产品架构与业务时序图](BACKEND_ARCHITECTURE.md)
- [Web 与 Tauri Client 产品架构与交互时序图](WEB_CLIENT_ARCHITECTURE.md)

后端、Web 或 Client 的模块边界、数据流、请求传输方式和更新流程发生变化时，必须同步更新对应图示，避免文档继续描述旧结构。
