# 客户端、权限与模块过渡交付核对

基准日期：2026-09-05。本文区分已完成的基础改造与随后追加的 Astra 整体视觉重设计；后者尚在实施时，不沿用上一版 UI 通过结论。

用户已确认本轮视觉基准为“中性灰白、精密克制”与“均衡信息密度”。最新整体设计由 Astra 负责，前端实施使用 Luna Max。已确认无调用的旧前端组件、旧样式与展示兼容层可清理；数据保护和 API 契约继续遵循完整方案。

## 需求与证据

| 需求 | 当前落实 | 验证边界 |
| --- | --- | --- |
| Figma 免费个人草稿验证 | 未找到可写个人 Draft，按约定取消 Figma 写入，使用书面设计规范 | 未写团队文件；没有声称交付 Figma 稿 |
| Eagle 素材筛选 | 已只读评估字体、图标和设计参考，未强制引入素材 | 按用户约定不做商业授权检查 |
| 三栏客户端、左栏三档 | 基础实现完成，正在按 Astra 新规范统一组件与页面 | 上一版浏览器 1023/1024/1280/1440 检查通过；新视觉需重新验收 |
| 默认博邦蓝亮色、亮暗与三品牌 | 本机持久化、挂载前恢复、浮层继承已实现 | 上一版三品牌对比度与浮层检查通过；新令牌需复验 |
| 服务器入口收进设置 | 已实现，正常连接不占首页；失败保留处理入口 | 本地浏览器检查通过 |
| 写权限隐含必要读取 | 用户写可读用户/角色，角色写可读角色/权限 | 后端、前端与接口测试覆盖；普通管理员真实窗口矩阵仍需补验 |
| 授权安全 | 自改角色、跨组织、超范围、非法 ID、终端超管、最后超管保护 | Go 全量与权限相关 race 测试通过 |
| 系统角色调整 | 仅 super_admin 锁定，历史 boss/operator 转普通角色，终端无默认角色 | 自动化验证幂等与授权保留；真实旧库副本仍需补验 |
| admin 保留与额外 Silence | 新库保留 admin 并经环境配置创建 Silence；旧库不补建/重置 | 初始化、bcrypt、缺配置失败关闭测试通过 |
| Silence 隐藏 | 托管标记账号从管理列表、搜索、分页与管理写操作排除；保留本人认证 | 已认证接口与新库浏览器列表检查通过 |
| 暂缓模块建表 | 从自动迁移移除计划中的模型并停用仓库种子；不删旧表数据 | 新库启动、统一 503、跨模块保护与旧表路径测试通过 |
| 统计兼容降级 | 200 响应带来源状态，前端不可用指标显示“—” | 接口与浏览器检查通过 |
| 文档与 API | API、Swagger 三产物、使用指南、Web/后端状态、Client 同步文档已更新 | 新视觉完成后再次同步 |
| 代理职责 | AGENTS.md 与八个代理配置已固化，并同步到主项目与功能工作树 | web_frontend 为 Luna Max；最新视觉设计按用户指定由 Astra 执行 |

## 技术验证记录

基础改造提交 `8e27aee`：Go 全量测试、go vet、权限/初始化/模块相关 race、nodynamic 专项、Web 27 项测试、Web/Client 前端构建通过。Tauri Rust 格式、检查和 26 项测试通过。macOS `.app` 构建并实际启动到本地内嵌启动页，未完成原生窗口中的已认证业务流程。

新增视觉改造涉及共享组件、语义令牌、样式分层与原生窗口标题；须在完成后追加本版证据，不能把基础提交的验证套用为新版通过。

## 成品验收场景

- 1024×680、1280、1440、1920 完整窗口；1439/1440 与窄屏断点两侧。
- 三档导航、详情查看/编辑/新增/切换/关闭、长客户名与地址、空值、足量列表行、滚动到底部操作区。
- 亮暗及三品牌下按钮、输入、选择、日期、菜单、表格、分页、空错状态、消息、Dialog/Drawer、图片文件区。
- 键盘顺序、Tab/Shift+Tab 循环、Escape、关闭恢复、减少动态偏好。
- 普通管理员和超级管理员的读取、角色/权限修改、不可授予原因、托管账号不可见。
- 全新库暂缓页与统计降级；真实旧库副本的角色转换、授权保留和业务表可用。
- Windows 10/11 Tauri、WebView2、100%/125%/150% 缩放、发现/切服、原生文件保存、窗口状态与真实业务流程由目标环境验收。

## 交付判定

当前新版测试仍有两项暗色按钮/停靠动画样式断言失败，需修复并复验；生产构建通过不代表产品验收通过。

2026-09-05：范围扩至从启动连接、登录到所有业务/系统模块和公共交互。当前已移除首页静态关注、入口计数、重复导航，统计移除无效搜索并隐藏不可用指标及空图表。Web 类型检查和生产构建通过；全流程视觉翻新与真实产品验收仍未完成，不得沿用前轮结果宣称本轮新设计完成。

UI 与产品审查不得存在未关闭 P0/P1。Windows、真实旧库和未执行的原生流程单独列为待验收，不标记为“整套已验收”或“已发布”。用户负责最终成品确认。
