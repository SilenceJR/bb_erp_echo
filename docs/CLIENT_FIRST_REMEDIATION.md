# Windows Tauri 客户端优先整体整改设计

> 产品确认日期：2026-09-01
> 适用范围：Go 服务端、Vue 共用业务层、Windows Tauri 客户端、桌面 Web 备用入口
> 本文是本轮整改的产品、技术、UI 与验收权威说明。

## 1. 产品定位与边界

- Windows Tauri 客户端是正式产品，目标环境为 Windows 10/11、1920×1080 显示器。
- Web 复用同一套 Vue 业务代码，作为内网桌面浏览器备用入口；Web 与 Tauri 的业务结果、权限、状态和错误语义必须一致。
- 当前按全新部署设计，不迁移、不识别、不回退旧数据库、旧 API、旧客户端或旧更新协议；被新契约取代的旧代码、文档和测试可以删除。
- 网络只支持 loopback 与 RFC1918 IPv4 内网地址，不支持公网、代理、macOS、移动端原生客户端或旧 WebView 兼容；平板/手机仅通过 Web 低频访问适配。
- “客户端优先”不等于用 Rust 重写业务页面。客户、仓库、任务、模具、权限等业务页面、DTO、校验和文案只维护一套 Vue 实现。
- 只有局域网发现、连接验证、本机文件保存、窗口生命周期和客户端更新属于 Tauri/Rust 平台能力。

## 2. 目标架构

| 层级 | 职责 | 约束 |
| --- | --- | --- |
| Go 服务 | 数据、权限、审计、库存/任务/模具状态机、发现响应 | 永远是业务最终裁决者 |
| 共享业务层 | 会话、权限、领域 API、页面状态、表单与错误 | 不直接调用 Tauri API |
| 模块注册表 | 菜单、标题、读写权限、页面懒加载 | 替代根组件的大量页面分支 |
| 领域模块 | 客户、供应商、仓库、任务、模具、统计、系统设置 | 每个领域独立状态与命令，不汇入巨型控制器 |
| 平台能力端口 | `Connection`、`Discovery`、`FileSave`、`WindowLeave`、`ClientUpdate` | Web/Tauri 分别提供适配器，业务组件只消费统一结果 |
| UI 原语 | 页面、筛选、表格、状态、Drawer、表单操作栏 | 只消费设计令牌，跨页面保持一致 |

采用的模式：

- **State Machine**：启动、发现、验证和连接流程。
- **Ports and Adapters / Strategy**：Web 与 Tauri 平台能力适配。
- **Facade**：页面只调用本领域动作，不感知传输与平台细节。
- **Registry**：模块、菜单、权限和懒加载入口集中声明。
- **Dirty Guard Registry**：统一聚合未保存状态并处理所有离开入口。
- **UI Primitives + Composition**：复用结构，不复用混杂业务逻辑的巨型页面。

不引入 Vue Router；继续使用中央导航协调器，但导航目标由模块注册表提供。

## 3. 客户端启动与局域网发现

### 3.1 启动状态机

```text
Booting
  -> Discovering
  -> Validating
  -> AutoConnected | SelectServer | ManualSetup
  -> LoginReady
```

- `Booting`：显示品牌启动页、客户端版本和“正在启动”。
- `Discovering`：Rust 在每个可用私网 IPv4 网卡发送一次 UDP 广播，固定收集 2.5 秒；不逐 IP、逐端口扫描。
- `Validating`：候选并发执行 `/ready` 与 ERP 身份校验。
- `AutoConnected`：只有一个验证通过的 `{origin, instance_id}` 复合候选时自动保存并进入登录；只有 origin 与实例 ID 均未变化时才可恢复已有会话。
- `SelectServer`：发现多个不同复合候选时禁止静默选择；同 ID、不同 origin 也完整保留并显示冲突清单，由用户明确选择。
- `ManualSetup`：零候选、候选验证失败、网络隔离或 UDP 被防火墙阻断时，保留“重新发现”和手动 IP 入口。

每次客户端启动执行一次发现。已保存服务仍验证通过但 UDP 无响应时，可以继续使用已保存服务，并提示自动发现未收到响应。

### 3.2 UDP 发现协议

默认 UDP 端口：`39080`。

客户端广播：

```json
{"kind":"discover","protocol":1,"nonce":"随机 128 位值"}
```

服务端单播响应：

```json
{
  "kind":"announce",
  "protocol":1,
  "nonce":"原样返回",
  "product":"bb-erp",
  "instance_id":"稳定 UUID",
  "server_name":"服务器名称",
  "http_port":8080
}
```

规则：

- 只接受协议版本正确、nonce 相同且 `product=bb-erp` 的响应。
- HTTP 主机只能采用 UDP 响应来源 IP，不能信任响应中的任意 URL；端口采用 `http_port`。
- 候选必须通过 `GET /ready`，要求 HTTP 200 且 `status=ready`。
- 候选还必须通过 `GET /api/v1/discovery/identity`，其 `instance_id` 必须与 UDP 响应一致。
- 身份接口只返回 `product`、`discovery_protocol`、`instance_id`、`server_name` 和 `server_version`，不得返回组织、账号、业务数据、更新地址或凭据。
- 仅对来源 IP、端口和 `instance_id` 完全相同的重复响应去重；同一 `instance_id` 若来自不同地址，按克隆身份冲突处理，禁止静默连接。
- 保存、当前连接、手动连接和候选选择统一使用规范化 `{origin, instance_id}`；任一字段变化都必须在业务会话挂载前清除 access/refresh token，并通过卸载工作台清除用户信息和领域缓存。

### 3.3 单服务约束

- 同一个发现窗口内出现两个或更多已验证的 `{origin, instance_id}` 复合候选即为冲突；同一 ID 出现在不同 origin 时按克隆冲突处理。
- 客户端冲突时不自动连接、不覆盖已保存连接，显示名称、IP、端口和版本。
- Go 服务数据库就绪后执行一次同协议预检；发现另一有效实例时拒绝启用本实例，并输出可诊断错误。
- 这是可检测的内网约束，不是跨 VLAN、防火墙或同时启动竞态下的绝对分布式锁；客户端后续发现冲突仍必须阻止静默连接。
- 服务器 Windows Private 网络放行 TCP ERP 端口和 UDP `39080` 入站；Public 网络不开放发现端口。

### 3.4 启动页设计

- 浅蓝灰全屏背景、居中品牌卡、现有高清 Logo、版本号、状态说明和明确操作。
- Logo 只在首次显示时执行不超过 240ms 的淡入和轻位移；尊重 `prefers-reduced-motion`。
- 扫描使用不确定进度条，不展示技术日志、扫描 IP 或虚假百分比。
- 零服务、扫描失败、候选验证失败是三个不同状态。
- 多服务卡片显示服务器名称、`IP:端口`、版本、“上次使用”标签和就绪状态。
- 手动输入接受内网 IPv4，可省略 `http://`，默认端口 `8080`；验证通过前不可保存。
- 状态变化使用 `aria-live=polite`，错误使用 `role=alert`；扫描结束后焦点进入结果标题，进入登录后焦点落到账号输入框。
- Web 不执行 UDP 发现，只验证当前站点服务；失败时提供重试和联系管理员提示。

## 4. 平台能力契约

### 4.1 Connection

- 只接受 loopback 或 RFC1918 IPv4 的 HTTP 源地址。
- `test` 必须执行 `/ready + discovery/identity`，验证失败的地址不可保存。
- 测试期间锁定输入；测试结果绑定规范化后的地址，输入变化即使结果失效。
- 登录页不再承担地址发现；只显示当前已验证服务和“更换服务器”。
- 工作台顶栏提供连接状态入口，可查看当前服务、重新发现或切换。

### 4.2 FileSave

- 客户模板、客户导出、服务端升级包及未来受保护下载统一调用 `FileSave`。
- Tauri 由 Rust 打开保存位置、处理取消与覆盖、携带当前认证读取同源受保护资源、流式写入临时文件并原子完成。
- 成功返回最终路径；取消、无权限、磁盘不足、网络失败和服务端错误必须分别反馈，写入完成前不得提示成功。
- Web 适配器保留 Blob 下载，但业务页面不得直接创建隐藏下载链接或判断运行平台。
- 更新中心 Web 入口只保留服务端升级包的真实受保护下载；桌面客户端更新只由 Tauri 原生面板执行，不展示没有有效路径的“完整 ZIP 故障恢复”动作。

### 4.3 WindowLeave

- `DirtyGuardRegistry` 支持多个表单、Drawer 和 Dialog 同时登记脏状态。
- 模块切换、退出登录、切换服务器、安装更新、浏览器刷新及 Tauri 标题栏关闭统一调用同一守卫。
- 保存中禁止离开；有未保存内容时明确说明影响，并默认保留编辑。
- Rust 拦截窗口关闭，等待 Vue 确认；确认后发放一次性关闭许可，取消则保持原状态。

### 4.4 ClientUpdate

- 只保留本轮定义的当前内网 Windows 更新链路；删除旧服务器 404 回退、旧 ZIP、RC、差分兼容和公网运行时分支。
- 保留签名、哈希、来源限制、临时文件、失败不覆盖当前可运行客户端和重启失败恢复等安全结果。
- 更新安装前必须通过统一离开守卫。
- 当前 API 只保留 `/updates/client/plan`、`/updates/client/tauri/...` 与 `/updates/client/artifacts/:sha256`；`/plan` 只接收真实版本、`windows-x86_64` 和 `nsis|portable`，`strategy` 固定为 `full`。
- 签名 payload 只允许 NSIS 与 portable 两个完整资源，未知字段及 `deltas` 一律拒绝；Windows 客户端只从已验证 ERP origin 下载，不读取上游发布地址。
- 正式发布必须用嵌入客户端的公钥实际验证服务端包、NSIS、portable 和 payload 的签名；私钥/公钥不匹配时禁止生成或发布稳定清单。
- portable 使用同目录暂存、旧 EXE 备份与新进程启动确认；不可写安装目录使用签名 NSIS。任一下载、签名、哈希、替换或重启失败都不得破坏当前可运行客户端。

## 5. 共享业务与代码拆分

### 5.1 控制器

原 3296 行 `useWorkspaceController` 的跨领域职责已完成本轮下沉；当前控制器为 1516 行，已拆为：

- 启动/连接状态与命令。
- 会话与权限。
- 应用壳与导航协调。
- 通用目录表单。
- 客户、仓库、任务、模具、统计等领域状态。

任务产品检索、临时建档、实时库存、详情日志、办公室动作和部门子任务动作已统一进入 `useWorkorderOperations`；通用目录查询、分页、可配置表单和供应商编辑已进入 `useDirectoryOperations`。控制器直接构造 `list/product/detail/action` 四切片 `workorderContext`，根只返回这一任务对象；`WorkspaceSession` 直接 provide，任务组件和非任务组件均不再从全量 Workspace 解构任务 facade。

### 5.2 页面

拆除巨型 `ModulePage`：

- `WarehousePage`
- `WorkorderPage`
- `MoldPage`
- `StatisticsPage`
- `SupplierPage`
- `DirectoryPage`（只用于字段和动作真正同构的系统基础目录）

客户、员工、部门继续使用专用页面。禁止用大量 `activeKey` 分支重新组成另一种通用巨页。

### 5.3 不可破坏的业务闭环

- 管理员初始化顺序：连接 → 登录 → 修改默认密码 → 部门 → 员工/成员 → 终端 → 账号/角色 → 账号归属。
- 所有仓库、库存和任务写入继续显式选择本次操作员工，不自动记忆或跨操作预填。
- 库存数量、成本、幂等、过账、冲销与余额以服务端裁决为准；失败保留可修改表单。
- 任务部门范围、状态流转、部分完成、待结案与强制完成原因以服务端裁决为准。
- 模具借出、归还、维修、保养只呈现服务端允许动作，成功后刷新状态与履历。
- 客户导入继续保持预览令牌、同文件确认、整批校验和原子写入。
- 无权限入口、只读状态、`403`、`409`、请求取消和旧响应覆盖保护不得回退。

## 6. UI 原语与样式治理

### 6.1 样式分层

```text
design-system.css
  -> Element Plus 主题映射
  -> components/ui 原语
  -> 业务页面组合
```

- `design-system.css` 是唯一设计令牌和 Element Plus 主题桥接源。
- UI 原语使用 scoped 样式；业务组件只保留领域特有布局，并全部消费 `--bb-*` 令牌。
- `styles.css` 只保留分层入口；旧 `--brand/--muted/--line/--ink` 等兼容别名已删除，`shell.css` 与 `feature-patterns.css` 全部使用正式 `--bb-*` 令牌，不把共享视觉值分散复制到页面。
- 禁止无作用域的 `table/th/td/.el-drawer__body` 业务覆盖。
- 除令牌定义外，不新增品牌色或状态色十六进制值。

### 6.2 统一原语

- `PageScaffold`、`PageHeader`
- `FilterBar`
- `DataTableShell`
- `PageState`
- `StatusTag`
- `SummaryStrip` / `MetricCard`
- `DrawerScaffold`
- `FormActionBar`

所有请求型页面必须覆盖首次加载、后台刷新、系统空数据、筛选空结果、错误、无权限、只读、提交中、成功、失败、依赖禁用和危险确认。

### 6.3 桌面布局

- 首次启动最大化，后续恢复上次窗口位置；最小窗口 `1024×680`。
- 桌面端使用固定顶部栏、左侧导航和独立滚动内容区；Web 窄屏使用可收起导航和单一可横向滚动表格，不维护桌面/移动双份列表模板。
- 动效采用 CSS 与 Motion 分层：高频 hover/focus/颜色变化使用 CSS；状态驱动、布局和 enter/exit 使用 `motion-v`；reduced motion 下移除位移、缩放和布局动画。
- 简单详情 Drawer 为 640px；仓库、任务和模具复杂 Drawer 为 760px，最大不超过视口 70%。
- Dialog 使用 480/560/680px；只滚动正文，footer 始终可达。
- 标准业务表格 48px 行高，纯文本目录可用 40px；表头固定、数字右对齐、操作列固定右侧。
- 1024px 紧凑桌面允许表格内部横向滚动，不允许应用壳产生横向滚动。

## 7. Windows 1080p 验收矩阵

| 环境 | 必验内容 |
| --- | --- |
| Windows 10/11，1920×1080，100% | 首启最大化、启动发现、登录、完整导航、表格、Drawer/Dialog、文件保存、客户端更新 |
| 同上，125% | 页面无横向裁切，筛选、操作列、抽屉底部操作和错误文案可达 |
| 同上，150% | 约 1280×720 逻辑工作区；核心操作可达，内容和 Drawer 独立滚动 |
| `1024×680` 最小窗口 | 导航、主操作、表格分页及弹层 footer 可达，无窗口级横向滚动 |
| 平板/手机 Web | 低频访问可完成登录、导航、列表查看和详情操作；导航可收起，表格允许内容区横向滚动，不改变 API/权限语义 |
| 唯一/零个/多个 ERP 服务 | 自动连接、手动降级、冲突阻断及异步结果防覆盖 |
| UDP 阻断但 TCP 可用 | 发现失败说明准确，手动验证仍可连接 |
| 管理员、办公室、仓库、部门、只读账号 | 权限入口、字段、按钮、成本信息和 API 拒绝一致 |
| 客户、库存、任务、模具完整流程 | 成功、校验、冲突、重复提交、断网、关闭、切服场景闭环 |
| Tauri 与 Web 同账号同数据 | 业务结果一致，仅本机保存、窗口和客户端更新存在平台差异 |

## 8. 交付门槛

### P0

- 未保存内容不会因标题栏关闭、退出、切服、更新或导航静默丢失。
- 服务发现的 nonce、来源 IP、产品、协议、`/ready` 和身份 ID 全部匹配；多实例绝不静默选择。
- 未就绪或身份不符的地址不可保存。
- Tauri 文件保存准确反馈成功、取消或失败，不误报成功。
- 发现与身份接口不暴露敏感业务数据或绕过认证。
- 原有权限、组织/部门、操作员工、库存、状态和幂等校验不产生部分写入回退。

### P1

- 模块页、任务领域和通用目录领域已拆分；任务组件使用四切片窄域 Context，工作台控制器已从 3296 行降至 1516 行并只承担会话、导航和领域组合。
- 设计令牌和 UI 原语已成为唯一共享视觉入口，旧兼容令牌搜索为零。
- 旧兼容、公网、代理、macOS、移动端原生客户端与旧更新代码和文档无残留；低频移动 Web 适配保持在共享 Vue/CSS 层。
- Go API 已完成 canonical 路径收敛：删除 `/api/v1/tasks`、单数 `/material`/`/product`/`/mold`、旧 `/api/v1/inventory`、`/api/v1/warehouse` 根别名和重复 `molds:*` 权限，仅保留 `/api/v1/warehouses`、`/api/v1/warehouse/items`、`/tabs` 及 `mold:*` 正式权限；同时移除图片权限回退，新库直接创建幂等部分唯一索引，账号/JWT 密码版本从 1 开始，管理员重置密码会撤销目标账号全部 refresh token。
- Go、Web、Tauri、API、用户与状态文档描述一致。
- Go 测试、Web/Client 构建、Rust 检查与测试全部通过。
- 完成并记录 Windows 10/11 真机发现、连接、关闭守卫、文件保存、更新和 1080p 缩放验收；静态构建不能替代。

## 9. 实施顺序

1. 建立发现/身份、平台能力端口、模块注册表和 UI 原语契约。
2. 实现 Go UDP 发现与身份接口、Rust 发现和启动状态机。
3. 实现连接验证、原生文件保存和统一窗口离开守卫。
4. 已拆分模块页与任务领域，并删除任务组件的全量 context 解构；其余领域按相同边界持续下沉。
5. 已完成分层样式入口、桌面单模板和正式 `--bb-*` 令牌迁移，旧兼容令牌已删除。
6. 同步 API、Swagger、用户、Web/Tauri、状态与部署文档。
7. 执行自动化验证、浏览器运行态验证和 Windows 真机验收；本轮 UI 由项目负责人亲自验收，不安排独立 UI 审查代理。
